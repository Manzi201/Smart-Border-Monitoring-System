[build]
  publish = "."
  command = "echo 'No build step required'"

[build.environment]
  NODE_VERSION = "20"
  PYTHON_VERSION = "3.13"

[[redirects]]
  from = "/api/*"
  to = "https://your-django-backend-url.com/api/:splat"
  status = 200
